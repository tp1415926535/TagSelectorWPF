<Window x:Class="Wpf.Net6.Test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf.Net6.Test" 
        xmlns:tsc="clr-namespace:TagSelectorWPF;assembly=TagSelectorWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="600" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <tsc:TagSelector x:Name="TagSelect" Source="{Binding SourceList}" Result="{Binding ResultList}" Tip="输入后回车添加项"
                         AllowInput="{Binding ElementName=AllowCheckbox,Path=IsChecked,UpdateSourceTrigger=PropertyChanged}" Margin="5">
            <tsc:TagSelector.Resources>
                <Style TargetType="ToggleButton" BasedOn="{StaticResource {x:Type ToggleButton}}"/>
            </tsc:TagSelector.Resources>
        </tsc:TagSelector>
        <StackPanel Grid.Row="1">
            <Button Content="Result" Click="Button_Click"/>
            <CheckBox x:Name="AllowCheckbox" Content="Allow User input text" IsChecked="True" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
